# Temporary asset register

The V2 foundation uses only `scope`, `play.fill`, `gearshape`, and `questionmark.circle` system symbols, a cyan `SKShapeNode` player marker, and asset-catalog colors. The empty App Icon slot is a development placeholder. All must be reviewed or replaced during the dedicated asset migration; no Java raster or generated artwork has been copied.

**Release blocker: Add an approved 1024×1024 Hookshot Hero app icon manually through Xcode or the asset pipeline.**
